# 大尾巴闪念 PC 版 v0.4.0 测试记录

> 日期：2026-08-12  
> 环境：Windows / AutoHotkey v2.0.26 / Obsidian 1.13.4  
> 结论：通过

## 1. 验收范围

- 三组快捷键均有独立开关，默认状态为开、关、开。
- 仅注册普通闪念和新建笔记两组默认启用热键，关闭的待办热键输入框灰显。
- 默认内容模式为 Markdown 代码块，语言标记为 `markdown`。
- 主界面和托盘菜单可以打开使用说明页面。
- 网页普通闪念显示“来自`域名`网页的剪藏。”并保留完整来源网址。
- 新建笔记后默认在闪念入口插入 Vault 相对 Wikilink。
- 新笔记回链开关和全部新增设置写入 UTF-8 `config.ini`。

## 2. 自动测试

| 项目 | 结果 |
| --- | --- |
| `tests/CoreTests.ahk` | `PASS=50 FAIL=0` |
| `--headless-check` | 通过 |
| `--ui-smoke-check` | 通过 |
| `--new-note-smoke-check` | 通过 |

自动测试覆盖网页域名句、顶格 Markdown 代码块、Vault 相对 Wikilink、锚点安全插入、三组开关配置持久化、默认注册数量、说明页内容和新笔记回链默认值。

## 3. Obsidian 联合验收

在 `C:\OBS\Damon\Codex-Temp\v0.4.0-验收` 中临时完成以下流程：

1. 创建“网页剪藏测试笔记”。
2. 构造 `[[Codex-Temp/v0.4.0-验收/网页剪藏测试笔记|网页剪藏测试笔记]]`。
3. 将链接安全插入临时闪念入口唯一锚点下方。
4. 通过 Obsidian CLI 打开并截图，确认链接可点击、`#闪念`、来源域名句、来源网址和字段层级正确。
5. 将两篇临时验收笔记移入 Obsidian 回收站，并删除空验收目录。

真实 `【MOC】闪念-随手记.md` 未写入验收内容，唯一锚点保持不变。
