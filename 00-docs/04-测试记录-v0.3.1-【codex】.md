# 大尾巴闪念 PC 版 v0.3.1 测试记录

> 日期：2026-08-12  
> 结论：代码块模式已支持网页加粗语法，围栏语言标记已改为 `markdown`。

## 期望输出

````markdown
```markdown
**Codex 这个用法一定要改，结果会好一个量级**

90% 的人都把 Codex 当神仙了……
```
````

## 回归结果

| 项目 | 结果 |
| --- | --- |
| `<strong>/<b>` 转 `**...**` | 通过 |
| `style="font-weight:700"` 转 `**...**` | 通过 |
| 代码块模式保留加粗 Markdown 源码 | 通过 |
| 围栏语言标记为 `markdown` | 通过 |
| 正文含三反引号时外层围栏自动加长 | 通过 |
| `tests/CoreTests.ahk` | `PASS=36 FAIL=0` |

## 边界

- 代码块内的 `**...**` 表示“已正确识别为 Markdown 加粗源码”，但 Obsidian 不会在围栏代码块内视觉渲染粗体。
- 如果需要视觉粗体，必须在设置中选择“智能 Markdown（保留加粗）”。
