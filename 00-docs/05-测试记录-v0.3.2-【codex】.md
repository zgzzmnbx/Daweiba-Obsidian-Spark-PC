# 大尾巴闪念 PC 版 v0.3.2 测试记录

> 日期：2026-08-12  
> 环境：Windows / AutoHotkey v2.0.26  
> 结论：通过

## 1. 本版验收范围

- Vault、闪念目标笔记、新建笔记目录在设置主界面中不可直接编辑。
- 三条路径只能通过各自“选择”按钮更新。
- 插入锚点显示在设置主界面中且不可直接编辑，只能通过“修改”按钮更新。
- 锚点保存到 `config.ini`，旧配置缺项时使用默认锚点。
- 保存前验证锚点非空且在目标笔记中恰好出现一次。
- 普通闪念和待办使用当前配置锚点写入。

## 2. 自动测试结果

| 项目 | 结果 |
| --- | --- |
| `tests/CoreTests.ahk` | `PASS=38 FAIL=0` |
| `--headless-check` | 通过 |
| `--ui-smoke-check` | 通过 |
| `--new-note-smoke-check` | 通过 |

核心测试新增覆盖自定义锚点插入和 `[Write] Anchor` UTF-8 配置持久化。UI 冒烟测试通过 Windows 控件样式检查，确认 Vault、闪念目标笔记、新建笔记目录、插入锚点四个字段均为只读，并确认界面锚点与已加载配置一致。

## 3. 安全说明

本次自动测试未向真实闪念笔记写入内容。当前真实目标笔记只用于只读配置校验；默认锚点检查通过后，设置 UI 冒烟测试正常退出。
