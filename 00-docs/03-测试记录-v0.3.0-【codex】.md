# 大尾巴闪念 PC 版 v0.3.0 测试记录

> 日期：2026-08-12  
> 结论：智能 Markdown、网页加粗识别、Markdown 代码块和纯文本模式已实现。

## 功能行为

| 模式 | 验证结果 |
| --- | --- |
| 智能 Markdown | CF_HTML `<strong>/<b>` 转为 `**加粗**`，段落和 `<br>` 换行保留 |
| Markdown 代码块（当前默认） | 生成标记为 `md` 的围栏代码块，正文含三反引号时外层自动升为四反引号 |
| 纯文本段落 | 不读取 HTML 格式，保留单换行和空行段落 |
| 待办 | 三种模式下首行仍为 Obsidian Tasks `- [ ]` |
| 新建笔记 | 智能 Markdown 可编辑；代码块模式预填完整围栏 |

## 回归结果

- `tests/CoreTests.ahk`：`PASS=33 FAIL=0`。
- HTML 加粗、段落、`&amp;` 实体解码：通过。
- 围栏反引号长度自适应：通过。
- UTF-8 `config.ini` 中当前 `Content.Format=code_block`：通过。
- `--headless-check`、`--ui-smoke-check`、`--new-note-smoke-check`：全部通过，无标准错误输出。

## 边界

- 加粗只在剪贴板存在 HTML 粗体标签时可识别；纯文本无格式证据时不猜测。
- Markdown 围栏代码块内容不会在 Obsidian 中渲染粗体，UI 已显示“代码块不渲染加粗”。
