# 大尾巴闪念 PC 版 v0.5.0 测试记录

> 日期：2026-08-12  
> 环境：Windows / AutoHotkey v2.0.26  
> 范围：剪贴板图片、Vault 附件复制、Obsidian 图片嵌入

## 1. 自动测试

| 检查项 | 结果 |
| --- | --- |
| `tests/CoreTests.ahk` | `PASS=62 FAIL=0` |
| `tests/ImageClipboardTests.ahk` | 原始 `CF_BITMAP` 成功编码为 PNG |
| `--headless-check` | 通过 |
| `--ui-smoke-check` | 通过 |
| `--new-note-smoke-check` | 通过 |

## 2. 覆盖行为

- 带引号的单张图片路径识别。
- `.png/.jpg/.jpeg/.gif/.bmp/.webp` 附件类型判断。
- `.obsidian/app.json` 中 `attachmentFolderPath=assets` 的自动读取。
- 外部图片复制、唯一附件名、Vault 相对路径和 `![[...]]` 生成。
- Vault 内已有图片直接复用，不重复复制。
- 普通图片闪念顶格嵌入；图片待办保持 Obsidian Tasks 层级和任务 ID。
- Windows 原始位图经 GDI+ 保存为 PNG，并恢复测试前完整剪贴板。
- 使用说明包含图片保存方式。

## 3. 安全边界

- 自动测试只写入项目 `Codex-Temp/` 夹具，不修改正式闪念笔记。
- 闪念落盘失败时删除本次新建图片；新建笔记取消时删除尚未被引用图片。
- 已在 Vault 内的图片标记为非新建附件，失败清理不得删除。

## 4. 真实 Vault 渲染验收

- 使用大尾巴本轮提供的实际 PNG 作为输入，成功复制到 Damon Vault 的 `assets` 并生成 `![[assets/大尾巴闪念图片-...png]]`。
- 临时验收笔记由 Obsidian 打开，图片按预期直接渲染，不再显示 Windows 临时路径代码块。
- 验收笔记、截图和附件均已移入 Obsidian 回收站，空验收目录已删除。
- 正式 `【MOC】闪念-随手记.md` 验收前后 SHA-256 均为 `7DB014F326860357487F6B3DE8F3DF45F9CA5A35F81A4DDA5A1396FE903D327D`，确认未被测试修改。
