# 大尾巴闪念 PC 版 v0.2.0 测试记录

> 日期：2026-08-12  
> 系统：Windows  
> 运行时：AutoHotkey v2.0.26  
> 结论：核心功能、UI、Edge 网址捕获和真实 Vault 验收通过；Chrome / Firefox 保留最终实机复核。

## 1. 自动回归

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| `tests/CoreTests.ahk` | 通过 | `PASS=24 FAIL=0` |
| 普通闪念格式 | 通过 | 标题、多行转换、来源网址、PC 块 ID |
| 待办格式 | 通过 | `- [ ]`、`#闪念 #待办`、任务 ID、空截止/提醒字段 |
| 锚点安全 | 通过 | 缺失锚点和重复锚点均拒绝写入 |
| UTF-8 安全写入 | 通过 | 保留无 BOM 状态，原子替换后验证唯一 ID |
| 新建笔记 | 通过 | 中文标题、YAML 来源、同名不覆盖、Vault 边界限制 |
| UTF-8 配置 | 通过 | 中文闪念路径和新建目录往返不乱码 |
| CF_HTML `SourceURL` | 通过 | 有来源和无来源样本均符合预期 |

## 2. UI 与快捷键注册

| 项目 | 结果 | 证据 |
| --- | --- | --- |
| 设置主窗口 | 通过 | `--ui-smoke-check` 真实创建 GUI 并返回有效 HWND |
| 新建笔记窗口 | 通过 | `--new-note-smoke-check` 验证正文和来源预填 |
| 三组动态快捷键 | 通过 | 启动期成功注册 `Ctrl+Alt+S/T/N`，无标准错误输出 |
| 配置检查按钮 | 通过 | 实际点击后显示“配置检查通过”且未写入真实笔记 |
| 中文路径显示 | 通过 | `【MOC】闪念-随手记.md` 和 `01-看板-日记-杂项` 无乱码 |

## 3. 浏览器来源

| 浏览器/路径 | 结果 | 说明 |
| --- | --- | --- |
| Edge UI Automation | 通过 | 返回有效 `http/https` 网址；完整网址未写入日志 |
| Edge 地址栏复制兜底 | 通过 | 返回有效 `http/https` 网址，剪贴板恢复无错误 |
| UIA 超时保护 | 通过 | 原始 UIA 在 Edge 出现过阻塞；改为独立 AHK worker + 650 ms 有界等待后，主保存流程不再受阻塞 |
| Chrome | 待实机复核 | 本机已安装；实现路径与 Chromium Edge 共用 |
| Firefox | 待实机复核 | 当前电脑未安装 Firefox |

## 4. 真实 Vault 验收

目标：`C:\OBS\Damon\【MOC】闪念-随手记.md`。

1. 验收前创建备份并记录 SHA-256：`B14EC42C8AD0139A7320BCF72EFB1CC8099AF4167EE98AD4A90D06292A5A4DEC`。
2. 写入唯一普通闪念和唯一待办，两者均含测试来源网址。
3. Obsidian CLI 读取结果：`OBSIDIAN_NORMAL=True`、`OBSIDIAN_TODO=True`、`OBSIDIAN_URL=True`。
4. 首轮验收发现 Obsidian 短暂占用文件时 `MoveFileExW` 可返回 Windows 错误 5；已加入错误 5/32/33 有界重试，复测后连续两次安全替换成功。
5. 验收块已清除，目标文件 SHA-256 恢复为验收前值，锚点数量仍为 1。
6. 在 `C:\OBS\Damon\01-看板-日记-杂项` 创建独立验收笔记，Obsidian CLI 已确认标题、正文和来源网址；验收后通过 Obsidian CLI 移入回收站。

## 5. 剩余人工验收

- 在 Chrome 实际网页中复制一段文字，分别触发普通闪念和待办，确认来源网址是复制页面而非切换后页面。
- 若后续安装 Firefox，重复 UI Automation 和地址栏兜底验收。
- 大尾巴按日常手感试用 `Ctrl+Alt+S/T/N`，确认是否与现有软件快捷键冲突。
