# 大尾巴闪念 PC 版 v0.5.1 测试记录

> 日期：2026-08-12
> 范围：Obsidian 图片默认显示宽度 300 像素

## 自动测试

| 项目 | 结果 |
| --- | --- |
| `tests/CoreTests.ahk` | `PASS=63 FAIL=0` |
| `tests/ImageClipboardTests.ahk` | 原始位图转 PNG、图片路径复制、`|300` 嵌入通过 |
| `--headless-check` | 退出码 0 |
| `--ui-smoke-check` | 退出码 0，使用说明包含 300 像素规则 |
| `--new-note-smoke-check` | 退出码 0 |

## Obsidian 实际显示验收

1. 验收前备份 `C:\OBS\Damon\【MOC】闪念-随手记.md` 并记录 SHA-256。
2. 在 Damon Vault 临时笔记中写入 `![[Codex-Temp/v0.5.1-图片宽度验收/sample.png|300]]`。
3. 通过 Obsidian CLI 打开笔记并读取渲染 DOM，图片实际显示宽度为 `300px`。
4. 临时笔记和图片已移入 Obsidian 回收站，临时目录已清除。
5. 正式闪念笔记验收前后 SHA-256 均为 `7D574C7D1E6C93CDAA752905739B56D7199E9F45DC7370207B622D4F3896030D`，未发生改动。

## 结论

v0.5.1 验收通过。普通闪念、图片待办和新建笔记统一使用 Obsidian 原生 `|300` 参数限制显示宽度；图片附件原文件不缩放、不转码、不改写。
