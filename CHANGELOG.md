# CHANGELOG

## v0.4.0 - 2026-08-12

### 新增

- 普通闪念、保存待办、新建笔记增加独立启用开关；默认启用第 1、3 项，默认关闭第 2 项。
- 主界面和托盘菜单增加“使用说明”，打开独立说明页面。
- 新建独立笔记后默认在闪念目标笔记锚点下插入可点击的 Vault 相对 Wikilink，可在设置中关闭。
- 网页普通闪念增加“来自`域名`网页的剪藏。”，域名只从已确认来源网址提取。

### 调整

- 内容格式默认保持 Markdown 代码块，围栏语言标记保持完整的 `markdown`。
- 只注册已启用的全局热键，冲突和非空校验也只针对启用项；关闭项的输入框灰显。
- 独立笔记创建成功但回链失败时，保留新笔记并报告部分成功和实际路径。

### 验证

- AutoHotkey 核心回归测试：50 通过，0 失败。
- 主脚本、设置 UI、新建笔记 UI 冒烟测试通过；默认只注册两组热键，使用说明页和默认开关状态自动检查通过。
- 临时 Vault 中完成“创建新笔记—生成相对 Wikilink—安全插入锚点—Obsidian 渲染”联合验收，随后将验收笔记移入 Obsidian 回收站。

## v0.3.3 - 2026-08-12

### 修复

- 普通闪念的 Markdown 代码块不再嵌套于列表，围栏和正文改为顶格，修复 Obsidian 长行软换行后突然向左跳的问题。
- 普通闪念的 `#闪念`、日期、来源网址、备注和块 ID 同步顶格，与常规独立代码块保持一致。
- 待办代码块仍嵌套于 `- [ ]`，不改变 Obsidian Tasks 识别结构。

### 验证

- AutoHotkey 核心回归测试：42 通过，0 失败。
- 新增普通代码块无列表缩进、元数据顶格、来源网址顶格、待办代码块保持嵌套测试。

## v0.3.2 - 2026-08-12

### 新增

- Vault、闪念目标笔记和新建笔记目录改为只读字段，分别只能通过“选择”按钮更新。
- 设置界面新增只读“插入锚点”字段，只能通过“修改”按钮更新。
- `config.ini` 新增 `[Write] Anchor`，旧配置缺少该项时自动使用默认锚点。

### 安全

- 保存和测试配置时验证锚点非空，且在目标笔记中恰好出现一次；失败时拒绝应用。
- 普通闪念和待办的实际写入改为使用已保存的锚点配置，不再固定使用默认常量。

### 验证

- AutoHotkey 核心回归测试：38 通过，0 失败。
- 主脚本、设置 UI、新建笔记 UI 冒烟测试全部通过；四个保护字段的 Windows 只读样式已自动检查。

### 发布

- 初始化 Git 仓库并备份到 `zgzzmnbx/Daweiba-Obsidian-Spark-PC` 的 `main` 分支。
- `v0.3.2` Release 附带 `AutoHotkey_2.0.18_setup-【按键精灵替代品-2024年8月】.exe`；该文件只作为运行环境安装包，不提交到源码分支。

## v0.3.1 - 2026-08-12

### 修复

- Markdown 代码块的语言标记由 `md` 改为完整的 `markdown`。
- 代码块模式不再只使用纯文本；现在也会从 CF_HTML 识别加粗，并保留为 `**...**` Markdown 源码。
- 加粗识别新增行内 `font-weight:bold/600–900` 兼容，覆盖 X 等网页不使用 `<strong>/<b>` 的情况。

### 验证

- AutoHotkey 核心回归测试：36 通过，0 失败。
- 粗体句“Codex 这个用法一定要改，结果会好一个量级”已通过行内 `font-weight:700` 转换测试。

## v0.3.0 - 2026-08-12

### 新增

- 设置界面新增“智能 Markdown、Markdown 代码块、纯文本段落”三选一内容格式。
- 智能 Markdown 从 CF_HTML 识别网页 `<strong>/<b>` 加粗，并保留段落、换行和列表。
- Markdown 代码块模式完整保留纯文本，围栏长度可避开正文中已有的连续反引号。
- 内容格式适用于普通闪念、待办和新建笔记，并持久化到 UTF-8 `config.ini`。
- 根据大尾巴实际验收，当前配置和新安装默认模式调整为 `code_block`；加粗渲染可手动切换到“智能 Markdown”。

### 验证

- AutoHotkey 核心回归测试：33 通过，0 失败。
- 设置 UI、内容格式默认选中、新建笔记 UI 和主脚本启动冒烟测试通过。
