# CHANGELOG 历史归档

## v0.3.1 - 2026-08-12

### 修复

- Markdown 代码块的语言标记由 `md` 改为完整的 `markdown`。
- 代码块模式不再只使用纯文本；现在也会从 CF_HTML 识别加粗，并保留为 `**...**` Markdown 源码。
- 加粗识别新增行内 `font-weight:bold/600–900` 兼容，覆盖 X 等网页不使用 `<strong>/<b>` 的情况。

### 验证

- AutoHotkey 核心回归测试：36 通过，0 失败。
- 粗体句“Codex 这个用法一定要改，结果会好一个量级”已通过行内 `font-weight:700` 转换测试。

## v0.3.0 - 2026-08-12

### 新增

- 设置界面新增“智能 Markdown、Markdown 代码块、纯文本段落”三选一内容格式。
- 智能 Markdown 从 CF_HTML 识别网页 `<strong>/<b>` 加粗，并保留段落、换行和列表。
- Markdown 代码块模式完整保留纯文本，围栏长度可避开正文中已有的连续反引号。
- 内容格式适用于普通闪念、待办和新建笔记，并持久化到 UTF-8 `config.ini`。
- 根据大尾巴实际验收，当前配置和新安装默认模式调整为 `code_block`；加粗渲染可手动切换到“智能 Markdown”。

### 验证

- AutoHotkey 核心回归测试：33 通过，0 失败。
- 设置 UI、内容格式默认选中、新建笔记 UI 和主脚本启动冒烟测试通过。

## v0.2.1 - 2026-08-12

### 修复

- 修复网页多行文字保存后被 ` / ` 压成一整段的问题。
- 普通闪念和待办现使用 Obsidian 列表续行：单换行保留为换行，空行保留为段落间隔。
- 段落内容仍归属同一条闪念或 Obsidian Tasks 待办，不影响标签、来源网址、Dataview 字段和块 ID。

### 验证

- AutoHotkey 核心回归测试：26 通过，0 失败。
- 多行普通闪念、多行待办、空行段落和 UI 启动冒烟测试通过。

## v0.2.0 - 2026-08-12

### 新增

- 交付 AutoHotkey v2 主脚本，不编译 EXE。
- 新增普通闪念、保存待办、新建笔记三组可配置全局快捷键。
- 新增设置主界面、新建笔记界面、托盘菜单、开机自启和日志轮换。
- 新增 CF_HTML `SourceURL`、独立进程 UI Automation 和地址栏复制三级来源网址策略。
- 新增唯一锚点检查、UTF-8 安全写入、临时文件原子替换、并发变化防护、文件占用重试和写后校验。
- 新增 UTF-8 `config.ini` 读写，避免 Windows INI API 导致中文路径乱码。

### 验证

- AutoHotkey 核心回归测试：24 通过，0 失败。
- 设置 UI 和新建笔记 UI 启动冒烟测试通过。
- Edge UI Automation 和地址栏复制兜底返回有效 `http/https` 网址。
- 真实 `C:\OBS\Damon` Vault 中的普通闪念、待办、来源网址和新建笔记均经 Obsidian CLI 验收通过。
